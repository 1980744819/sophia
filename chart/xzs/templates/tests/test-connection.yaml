apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xzs.fullname" . }}-test-connection"
  labels:
    {{- include "xzs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xzs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
